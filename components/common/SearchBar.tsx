const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search for workflows and models..."
      className="h-8 w-full bg-transparent rounded
        text-sm px-auto mb-6"
    />
  );
};

export default SearchBar;
